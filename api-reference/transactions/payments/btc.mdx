---
title: "BTC Payment"
openapi: "POST /api/v1/transactions/payments/btc"
description: "Create a BTC QR code for payment"
---

<ParamField body="amount_in_brl" type="string" required>
  Payment amount in Brazilian Reais (BRL).
</ParamField>

<ParamField body="expires_in" type="integer" required>
  Expiration time in seconds. Minimum: 120 seconds (2 minutes), Maximum: 1800 seconds (30 minutes).
</ParamField>

<ParamField body="description" type="string">
  Payment description (optional, max 255 characters).
</ParamField>

<ParamField body="idempotency_key" type="string" required>
  Unique key to prevent duplicate payments. This key also serves for idempotency - using the same `idempotency_key` will return the same payment.
</ParamField>

<ParamField body="network" type="string" required>
  Blockchain network to use for the payment.
  Allowed values: `lightning`, `bitcoin`.
  
  <Note>
    For payments, the recommended network is `lightning` as it provides faster confirmations (usually 1-3 seconds) compared to on-chain Bitcoin transactions.
  </Note>
</ParamField>

<RequestExample>

```bash cURL
curl --request POST \
  --url https://api.monkei.co/api/v1/transactions/payments/btc \
  --header 'Content-Type: application/json' \
  --header 'X-API-Key: YOUR_API_KEY' \
  --data '{
  "amount_in_brl": "100.50",
  "expires_in": 1800,
  "description": "Payment for services",
  "idempotency_key": "btc_test_001",
  "network": "lightning"
}'
```

```javascript JavaScript
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-API-Key': 'YOUR_API_KEY'
  },
  body: JSON.stringify({
    amount_in_brl: '100.50',
    expires_in: 1800,
    description: 'Payment for services',
    idempotency_key: 'btc_test_001',
    network: 'lightning'
  })
};

fetch('https://api.monkei.co/api/v1/transactions/payments/btc', options)
  .then(response => response.json())
  .then(response => console.log(response))
  .catch(err => console.error(err));
```

</RequestExample>

<ResponseExample>

```json
{
    "success": true,
    "data": {
        "id": "019abcf3-11b5-7d12-8719-6bd85796aba9",
        "amount_in_brl": "100.50",
        "amount_in_crypto": "0.00021405",
        "platform_fee": "0",
        "status": "PENDING",
        "expires_in": 1800,
        "qr_code": "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==",
        "qr_code_url": ""
    },
    "message": "Resource created successfully"
}
```

</ResponseExample>

## Response Fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | string (UUID) | Unique payment transaction identifier |
| `amount_in_brl` | string | Payment amount in Brazilian Reais |
| `amount_in_crypto` | string | Payment amount in BTC (crypto) |
| `platform_fee` | string | Platform fee charged for the transaction (optional) |
| `status` | string | Payment status: `PENDING`, `PROCESSING`, `COMPLETED`, `FAILED`, `CANCELED` |
| `expires_in` | integer | Expiration time in seconds |
| `qr_code` | string | QR code image in base64 format (for display) |
| `qr_code_url` | string | BTC payment string (Lightning invoice BOLT11 for `lightning` network, or Bitcoin address for `bitcoin` network) |

<Note>
  The `qr_code` field contains a base64-encoded image that can be directly displayed in your application. The `qr_code_url` field contains the payment string that can be used by payment applications to process the payment.
</Note>

<Warning>
  The `qr_code_url` payment string (e.g., Lightning invoice `lnbc214050n1p5jvgvd...`) was created specifically for this payment transaction and should **not be reused** for other payments. Each payment must use its own unique payment string.
</Warning>

<Note>
  The `amount_in_crypto` field shows the equivalent amount in BTC based on the exchange rate at the time of transaction creation. This value is locked and remains fixed until the payment expires or is completed, regardless of cryptocurrency price fluctuations.
</Note>

<Note>
  When using the `lightning` network, payments are typically confirmed within 1-3 seconds. The `bitcoin` network uses on-chain transactions which generally take 1-3 minutes for confirmation.
</Note>

## Status Codes

| Code | Description |
|------|-------------|
| `201` | BTC QR code created successfully |
| `400` | Bad request - invalid parameters or validation error |
| `401` | Unauthorized - invalid or missing API key |
| `500` | Internal server error |



<Warning>
  The `expires_in` value must be between 120 seconds (2 minutes) and 1800 seconds (30 minutes). Values outside this range will result in a validation error. The maximum is limited to 30 minutes due to cryptocurrency volatility.
</Warning>

<Warning>
  Only `lightning` and `bitcoin` networks are supported. Using any other network value (e.g., `ethereum`, `polygon`) will result in an error.
</Warning>

<Note>
  The `amount_in_crypto` value is locked at the time of transaction creation based on the current exchange rate. This value remains fixed until the payment expires or is completed, regardless of cryptocurrency price fluctuations.
</Note>
