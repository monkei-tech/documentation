---
title: "Latest Exchange Rate"
openapi: "POST /api/v1/exchange/latest"
description: "Retrieve the latest exchange rate quote for a fiat and cryptocurrency pair"
---

Retrieve the latest valid exchange rate quote for a specific fiat currency and cryptocurrency pair, including expiration information and market data.

## Endpoint

```http
POST /api/v1/exchange/latest
```

## Request Body

<ParamField body="fiat_currency" type="string" required>
  The fiat currency code. Supported values: `BRL`, `USD`.
</ParamField>

<ParamField body="crypto_currency" type="string" required>
  The cryptocurrency code (e.g., `BTC`, `USDT`).
</ParamField>

<RequestExample>

```bash cURL
curl --request POST \
  --url https://api.monkei.co/api/v1/exchange/latest \
  --header 'Content-Type: application/json' \
  --header 'X-API-Key: YOUR_API_KEY' \
  --data '{
  "fiat_currency": "BRL",
  "crypto_currency": "BTC"
}'
```

```javascript JavaScript
const options = {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-API-Key': 'YOUR_API_KEY'
  },
  body: JSON.stringify({
    fiat_currency: 'BRL',
    crypto_currency: 'BTC'
  })
};

fetch('https://api.monkei.co/api/v1/exchange/latest', options)
  .then(response => response.json())
  .then(response => console.log(response))
  .catch(err => console.error(err));
```

```typescript TypeScript
interface QuoteRequest {
  fiat_currency: string;
  crypto_currency: string;
}

interface QuoteResponse {
  success: boolean;
  data: {
    quote: {
      id: string;
      symbol: string;
      exchange_rate: string;
      fiat_currency: string;
      crypto_currency: string;
      decimal_factor: string;
      consulted_at: string;
      created_at: string;
      updated_at: string;
    };
  };
  message: string;
}

async function getLatestQuote(fiatCurrency: string = 'BRL', cryptoCurrency: string = 'BTC'): Promise<QuoteResponse> {
  const request: QuoteRequest = {
    fiat_currency: fiatCurrency,
    crypto_currency: cryptoCurrency
  };

  const response = await fetch('https://api.monkei.co/api/v1/exchange/latest', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'X-API-Key': 'YOUR_API_KEY'
    },
    body: JSON.stringify(request)
  });

  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }

  return await response.json();
}

getLatestQuote()
  .then(data => console.log(data))
  .catch(error => console.error('Error:', error));
```

```python Python
import requests
import json

url = "https://api.monkei.co/api/v1/exchange/latest"

headers = {
    "Content-Type": "application/json",
    "X-API-Key": "YOUR_API_KEY"
}

payload = {
    "fiat_currency": "BRL",
    "crypto_currency": "BTC"
}

# Example with USD
payload_usd = {
    "fiat_currency": "USD",
    "crypto_currency": "USDT"
}

response = requests.post(url, headers=headers, json=payload)

if response.status_code == 200:
    data = response.json()
    print(json.dumps(data, indent=2))
else:
    print(f"Error: {response.status_code}")
    print(response.text)
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

type QuoteRequest struct {
    FiatCurrency  string `json:"fiat_currency"`
    CryptoCurrency string `json:"crypto_currency"`
}

type QuoteResponse struct {
    Success bool `json:"success"`
    Data    struct {
        Quote struct {
            ID             string `json:"id"`
            Symbol         string `json:"symbol"`
            ExchangeRate   string `json:"exchange_rate"`
            FiatCurrency   string `json:"fiat_currency"`
            CryptoCurrency string `json:"crypto_currency"`
            DecimalFactor  string `json:"decimal_factor"`
            ConsultedAt    string `json:"consulted_at"`
            CreatedAt      string `json:"created_at"`
            UpdatedAt      string `json:"updated_at"`
        } `json:"quote"`
    } `json:"data"`
    Message string `json:"message"`
}

func main() {
    url := "https://api.monkei.co/api/v1/exchange/latest"
    
    requestBody := QuoteRequest{
        FiatCurrency:  "BRL",
        CryptoCurrency: "BTC",
    }
    
    // Example with USD
    requestBodyUSD := QuoteRequest{
        FiatCurrency:  "USD",
        CryptoCurrency: "USDT",
    }
    
    jsonData, err := json.Marshal(requestBody)
    if err != nil {
        fmt.Printf("Error marshaling JSON: %v\n", err)
        return
    }
    
    req, err := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
    if err != nil {
        fmt.Printf("Error creating request: %v\n", err)
        return
    }
    
    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("X-API-Key", "YOUR_API_KEY")
    
    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        fmt.Printf("Error making request: %v\n", err)
        return
    }
    defer resp.Body.Close()
    
    body, err := io.ReadAll(resp.Body)
    if err != nil {
        fmt.Printf("Error reading response: %v\n", err)
        return
    }
    
    var quoteResponse QuoteResponse
    if err := json.Unmarshal(body, &quoteResponse); err != nil {
        fmt.Printf("Error unmarshaling response: %v\n", err)
        return
    }
    
    fmt.Printf("Response: %+v\n", quoteResponse)
}
```

```php PHP
<?php

$url = 'https://api.monkei.co/api/v1/exchange/latest';

$data = [
    'fiat_currency' => 'BRL',
    'crypto_currency' => 'BTC'
];

// Example with USD
$dataUSD = [
    'fiat_currency' => 'USD',
    'crypto_currency' => 'USDT'
];

$options = [
    'http' => [
        'header' => [
            'Content-Type: application/json',
            'X-API-Key: YOUR_API_KEY'
        ],
        'method' => 'POST',
        'content' => json_encode($data)
    ]
];

$context = stream_context_create($options);
$response = file_get_contents($url, false, $context);

if ($response === false) {
    echo "Error making request\n";
} else {
    $result = json_decode($response, true);
    echo json_encode($result, JSON_PRETTY_PRINT);
}

// Alternative using cURL
$ch = curl_init($url);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Content-Type: application/json',
    'X-API-Key: YOUR_API_KEY'
]);

$response = curl_exec($ch);
$httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
curl_close($ch);

if ($httpCode === 200) {
    $result = json_decode($response, true);
    echo json_encode($result, JSON_PRETTY_PRINT);
} else {
    echo "Error: HTTP $httpCode\n";
}
```

```rust Rust
use reqwest;
use serde::{Deserialize, Serialize};
use serde_json::json;

#[derive(Serialize)]
struct QuoteRequest {
    fiat_currency: String,
    crypto_currency: String,
}

#[derive(Deserialize, Debug)]
struct QuoteResponse {
    success: bool,
    data: QuoteData,
    message: String,
}

#[derive(Deserialize, Debug)]
struct QuoteData {
    quote: Quote,
}

#[derive(Deserialize, Debug)]
struct Quote {
    id: String,
    symbol: String,
    exchange_rate: String,
    fiat_currency: String,
    crypto_currency: String,
    decimal_factor: String,
    consulted_at: String,
    created_at: String,
    updated_at: String,
}

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = reqwest::Client::new();
    
    let request_body = json!({
        "fiat_currency": "BRL",
        "crypto_currency": "BTC"
    });
    
    // Example with USD
    let request_body_usd = json!({
        "fiat_currency": "USD",
        "crypto_currency": "USDT"
    });
    
    let response = client
        .post("https://api.monkei.co/api/v1/exchange/latest")
        .header("Content-Type", "application/json")
        .header("X-API-Key", "YOUR_API_KEY")
        .json(&request_body)
        .send()
        .await?;
    
    if response.status().is_success() {
        let quote_response: QuoteResponse = response.json().await?;
        println!("{:#?}", quote_response);
    } else {
        eprintln!("Error: HTTP {}", response.status());
    }
    
    Ok(())
}
```

</RequestExample>

## Response

<ResponseExample>

```json
{
    "success": true,
    "data": {
        "quote": {
            "id": "019ae1e8-ed99-78a6-b641-5a2e52fb5417",
            "symbol": "BTC/BRL",
            "exchange_rate": "491849",
            "fiat_currency": "BRL",
            "crypto_currency": "BTC",
            "decimal_factor": "8",
            "consulted_at": "2025-12-03T01:52:10.137565Z",
            "created_at": "2025-12-03T01:52:10.137567Z",
            "updated_at": "2025-12-03T01:52:10.137567Z"
        }
    },
    "message": "Operation completed successfully"
}
```

</ResponseExample>

## Response Fields

| Field | Type | Description |
|-------|------|-------------|
| `quote` | object | Quote information object |

### Quote Object

| Field | Type | Description |
|-------|------|-------------|
| `id` | string (UUID) | Unique quote identifier |
| `symbol` | string | Quote symbol in format "CRYPTO/FIAT" (e.g., "BTC/BRL", "USDT/USD") |
| `exchange_rate` | string | Current exchange rate |
| `fiat_currency` | string | Fiat currency code (e.g., "BRL", "USD") |
| `crypto_currency` | string | Cryptocurrency code (e.g., "BTC", "USDT") |
| `decimal_factor` | string | Decimal precision factor for the cryptocurrency |
| `consulted_at` | string (ISO 8601) | Timestamp when the quote was consulted |
| `created_at` | string (ISO 8601) | Quote creation timestamp |
| `updated_at` | string (ISO 8601) | Quote last update timestamp |

<Note>
  The `exchange_rate` value represents how many units of the fiat currency are needed to buy one unit of the cryptocurrency. For example, if `exchange_rate` is "491849" for BTC/BRL, it means 1 BTC costs 491,849 BRL.
</Note>

<Note>
  The `symbol` field follows the format "CRYPTO/FIAT" (e.g., "BTC/BRL", "USDT/USD") to clearly identify the currency pair.
</Note>

## Status Codes

| Code | Description |
|------|-------------|
| `200` | Quote retrieved successfully |
| `400` | Bad request - invalid parameters or missing required fields |
| `401` | Unauthorized - invalid or missing API key |
| `404` | Not found - no quote available for the specified currency pair |
| `500` | Internal server error |


<Note>
  Exchange rates are updated regularly. Always use the latest quote for accurate conversions in transactions.
</Note>

<Note>
  The `exchange_rate` and other numeric fields are returned as strings to maintain precision. When performing calculations, parse them as decimal numbers using appropriate libraries to avoid floating-point precision issues.
</Note>
